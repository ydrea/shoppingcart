import styled from 'styled-components'


export const Wrapper = styled.div`
    margin: 5px;

.cart-button {
    display: flex;
    flex-direction: row;
    position: -webkit-sticky;
    position: sticky;
    // display: none
}

`;