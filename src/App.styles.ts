import styled from 'styled-components'


export const Wrapper = styled.div`
    margin: 5px;

.cart-button {
    margin-top: 20px;
    padding: 20px;

    display: flex;
    flex-direction: row;
    position: fixed;
    // display: none
}

`;